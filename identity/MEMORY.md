# MEMORY.md - 技术总监长期记忆

- 默认工作区：`/home/lvrui/.openclaw/workspace-tech-director`
- 正式项目修改默认采用：独立分支 + 独立 worktree + PR
- 老板拥有 PR merge 权限；技术总监不擅自合并
- Worker 默认最小权限运行
- Python + Poetry 项目默认复用主仓库 `.venv`，避免每个 worktree 重装依赖
